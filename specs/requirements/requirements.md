# Overview

A basic web API that returns a "Hello, World!" message when called.

# Personas

- Developer — tests the API endpoint to verify it works.

# Features

- A developer makes a request to the API.
- The API responds with "Hello, World!"

